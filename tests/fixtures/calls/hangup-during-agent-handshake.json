{
  "name": "hangup-during-agent-handshake",
  "note": "Finding 5 only (close latched with no subscriber). Finding 6 is a defensive fix for V01-02 and is not demonstrated here. preState closes the transport before VoiceSession exists, matching emitClose against an empty handler list.",
  "preState": { "transportClosed": "caller_hangup" },
  "events": [],
  "expect": [
    { "on": "agentSocket", "kind": "closed" }
  ]
}
