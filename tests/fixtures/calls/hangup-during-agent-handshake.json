{
  "name": "hangup-during-agent-handshake",
  "note": "Findings 5 and 6. preState closes the transport before VoiceSession exists; onClose must replay to the late subscriber, closeRequested must close the socket that opens afterwards, and start() must reject. No Settings (or any other) frame may go out.",
  "preState": { "transportClosed": "caller_hangup" },
  "events": [
    { "at": 0, "from": "agentSocket", "kind": "open" }
  ],
  "expect": [
    { "on": "agentSocket", "kind": "closed" }
  ],
  "forbid": [
    { "on": "agentSocket", "kind": "send", "before": 0 }
  ],
  "count": [
    { "on": "agentSocket", "kind": "send", "exactly": 0 }
  ],
  "assertTimersClearAfter": true
}
