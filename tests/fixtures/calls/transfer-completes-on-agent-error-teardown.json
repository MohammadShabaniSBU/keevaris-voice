{
  "name": "transfer-completes-on-agent-error-teardown",
  "note": "Regression for the transportGone flag. Same arm as the hangup fixture, but the close comes from the agent socket. transportGone stays false; teardown dispatches.",
  "delegation": {
    "response": {
      "text": "I am transferring you now.",
      "transfer": true,
      "destination": "main_line"
    }
  },
  "events": [
    { "at": 0, "from": "agentSocket", "kind": "open" },
    { "at": 1, "from": "agentSocket", "kind": "control", "message": { "type": "Welcome", "request_id": "req_1" } },
    { "at": 2, "from": "agentSocket", "kind": "control", "message": { "type": "SettingsApplied" } },
    {
      "at": 100,
      "from": "agentSocket",
      "kind": "control",
      "message": {
        "type": "FunctionCallRequest",
        "functions": [
          {
            "id": "fc_1",
            "name": "ask_keevaris",
            "arguments": "{\"query\":\"I want to talk to a person\"}",
            "client_side": true
          }
        ]
      }
    },
    { "at": 150, "from": "agentSocket", "kind": "error", "reason": "agent_error" }
  ],
  "expect": [
    { "on": "session", "kind": "session.transfer_teardown" },
    { "on": "transport", "kind": "transfer" }
  ],
  "count": [
    { "on": "session", "kind": "session.transfer_abandoned", "exactly": 0 }
  ]
}
